import { AlertTriangle, ArrowRightLeft, Clock3, Gauge, ShieldAlert } from 'lucide-react'
import { Card, StatRow } from '@/components/ui/card'
import type { WorkflowStepMock } from '@/data/mock/audit-workflows'

export function WorkflowSummary({steps}:{steps:WorkflowStepMock[]}){const waiting=steps.filter(s=>s.waiting!=='0 min').length;return <aside className="space-y-4"><Card className="p-4"><h3 className="mb-4 text-xs font-semibold">Process Summary</h3><div className="space-y-2"><StatRow label="Workflow steps" value={String(steps.length)}/><StatRow label="Departments" value="3"/><StatRow label="Systems" value="3"/><StatRow label="Approval steps" value={String(steps.filter(s=>s.approval).length)}/><StatRow label="Evidence items" value={String(steps.reduce((n,s)=>n+s.evidence,0))}/></div></Card><Card className="p-4"><h3 className="mb-3 text-xs font-semibold">Analysis Areas</h3>{[['Bottlenecks',waiting,Clock3],['Manual Work',3,Gauge],['Handoffs',4,ArrowRightLeft],['Control Gaps',2,ShieldAlert],['Ownership Ambiguity',1,AlertTriangle]].map(([label,count,I])=>{const Icon=I as typeof Clock3;return <div key={label as string} className="flex items-center justify-between border-b border-border py-2.5 last:border-0"><span className="flex items-center gap-2 text-[10px]"><Icon className="size-3.5 text-brand"/>{label as string}</span><b className="text-[10px]">{String(count)}</b></div>})}</Card><Card className="p-4"><h3 className="text-xs font-semibold">Process Boundary</h3><p className="mt-3 text-[10px] text-muted-foreground">Starts</p><p className="text-[11px] font-semibold">Purchase Request Submitted</p><p className="mt-3 text-[10px] text-muted-foreground">Ends</p><p className="text-[11px] font-semibold">Supplier Payment Completed</p></Card></aside>}
