import { AlertTriangle, CheckCircle2, Clock3, MoreHorizontal } from 'lucide-react'
import type { WorkflowStepMock } from '@/data/mock/audit-workflows'

export function WorkflowNode({step,index,selected,onSelect}:{step:WorkflowStepMock;index:number;selected:boolean;onSelect:()=>void}){return <button onClick={onSelect} className={`relative w-56 shrink-0 rounded-lg border bg-background p-4 text-left shadow-sm outline-none transition focus-visible:ring-2 focus-visible:ring-brand ${selected?'border-brand ring-1 ring-brand':'border-border hover:border-foreground/30'}`}><div className="flex items-start justify-between"><span className="flex size-7 items-center justify-center rounded-full bg-brand text-[10px] font-bold text-white">{index+1}</span><MoreHorizontal className="size-4 text-muted-foreground"/></div><p className="mt-3 text-xs font-semibold">{step.name}</p><p className="mt-1 text-[10px] text-muted-foreground">{step.owner} · {step.department}</p><div className="mt-3 flex items-center justify-between border-t border-border pt-3 text-[9px] text-muted-foreground"><span className="flex items-center gap-1"><Clock3 className="size-3"/>{step.duration}</span><span>{step.system}</span></div><div className="mt-2 flex items-center gap-2 text-[9px]">{step.control&&<span className="flex items-center gap-1 text-success"><CheckCircle2 className="size-3"/> Control</span>}{step.painPoint&&<span className="flex items-center gap-1 text-brand"><AlertTriangle className="size-3"/> Pain point</span>}</div></button>}
