# API boundary

Route handlers will be added here only when a concrete backend is approved. Planned resource boundaries are audits, assessments, frameworks, templates, findings, recommendations, evidence, users, organisations, and Atlas.

UI components must call TRACE domain/application services rather than communicating with Atlas directly.
